name: Leaky Token Demo

on:
  push:
    branches:
      - main

jobs:
  leak-token:
    runs-on: ubuntu-latest
    environment: my_env1  # 👈 This connects to your configured environment

    env:
      API_TOKEN: ${{ secrets.TOKEN }} # 👈 Pulls in the variable from the environment

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Simulate token leak in logs
        run: |
          echo "🕵️ Simulating token use..."
          echo "Token used for deployment: $API_TOKEN"  # Leak in logs

      - name: Simulate leak to external server
        run: |
          python test.py
