name: Simple Echo Workflow

on: 
  push:
    branches:
      - main

jobs:
  echo-job:
    runs-on: ubuntu-latest
    environment: my_env
    steps:
      - name: Print a message
        run: echo "111111111111"
