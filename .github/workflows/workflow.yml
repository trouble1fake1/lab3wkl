name: Leaky Token Demo

on:
  push:
    branches:
      - main  # or use `**` to run on all branches

jobs:
  leak-token:
    runs-on: ubuntu-latest

    env:
      API_TOKEN: $TOKEN

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Simulate token leak in logs
        run: |
          echo "🕵️ Simulating token use..."
          echo "Token used for deployment: $TOKEN"  # Leak in logs

      - name: Simulate leak to external server
        run: |
          python test.py
